【查看APP包信息】：
aapt dump badging D:\Android-SDK-Windows\app\58Coin.apk
【启动AppiumServer】：
webdriver.Remote("http://127.0.0.1:4723/wd/hub", Capabilities)
【滑动界面】：
driver.swipe(x,y,x1,y1,time)  例：driver.swipe(700, 400, 50, 400, duration=sleep(1))
appium 1.7 以后swipe滑动不能正常使用
解决方法就是duration，直接传入sleep，而不是数字
from time import sleep
swipe(int(width)/2,int(height)/2,int(width)/2,int(height)/4,duration=sleep(1))
【获取屏幕大小】：
driver.get_window_size()

['NATIVE_APP']
